@ hello.lp — The canonical Lang.P hello world program.

@ Define a function that greets someone by name.
function greet(name),
    print "Hello " with name with "!".
.

@ Call the function with a name.
greet("World").
