@ server.lp — Network module stub (v0.2 beta)
@
@ The full HTTP Server API (Server, on server.request) is planned for a future release.
@ This example verifies that `use network.` loads the built-in module stub today.

use network.

print "Network version: " with network.version.
print "Lang.P HTTP server — coming in a future release.".
