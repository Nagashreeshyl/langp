[package]
name = "langp-lexer"
description = "Lexer for the Lang.P programming language"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[lib]
name = "langp_lexer"
path = "src/lib.rs"

[dev-dependencies]
glob = "0.3"
