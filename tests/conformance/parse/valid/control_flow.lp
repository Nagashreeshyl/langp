@ valid: control flow keywords
if score >= 60,
    print "Pass".
otherwise if score >= 40,
    print "Retry".
otherwise,
    print "Fail".
..

repeat forever,
    process().
..

result = wait for fetch(url).
